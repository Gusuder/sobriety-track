window.APP_CONFIG = {
  // Optional: if set, web UI can render Google OAuth button without localStorage hack.
  // Example: "371943493630-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com"
  googleClientId: ""
};
